import songs from "./songs.json";

export default songs;


/* This was used when I was testing different data sources.
*  Simply change the import to the data source you want to use. */

// I imported the data as a csv and ran scripts/convertData.ts to convert it to songs.json
// Use the scripts as a reference if importing new data but they may have to be tweaked to be fit the data's needs